/** \brief  Recibe dos numeros flotantes ingresado por el usuario y los suma
 *
 * \param float valor del primer operando
 * \param float valor del segundo operando
 * \return float es el resultado de la suma
 *
 */

float suma(float ,float );

/** \brief Recibe dos numeros flotantes ingresado por el usuario y los resta
 *
 * \param float valor del primer operando
 * \param float valor del segundo operando
 * \return float es el resultado de la resta
 *
 */

float resta(float ,float );

/** \brief Recibe dos numeros flotantes ingresado por el usuario y los divide
 *
 * \param float valor del primer operando
 * \param float valor del segundo operando
 * \return float es el resultado de la division
 *
 */

float division(float ,float );

/** \brief Recibe dos numeros flotantes ingresado por el usuario y los multiplica
 *
 * \param float valor del primer operando
 * \param float valor del segundo operando
 * \return float es el resultado de la multiplicacion
 *
 */

float multiplicar(float ,float );

/** \brief Recibe un numero entero ingresado por el usuario y calcula el factorial
 *
 * \param int valor del operando
 * \param
 * \return int es el resultado del factorial
 *
 */

int factorial(int);
